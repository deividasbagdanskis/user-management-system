{% extends 'base.html.twig' %}

{% block title %}New User | User manager{% endblock %}

{% block body %}
<div class="container">
    <h1 class="my-3">Create a new user</h1>
    {% if usernameError %}
        <div class="alert alert-danger my-3">{{ usernameError }}</div>
    {% endif %}

    {{ include('user/_form.html.twig') }}

    <a href="{{ path('users-index') }}">back to list</a>
</div>

{% endblock %}
