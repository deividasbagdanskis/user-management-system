<form class="mt-3" method="post" action="{{ path('groups-delete', {'id': group.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ group.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
